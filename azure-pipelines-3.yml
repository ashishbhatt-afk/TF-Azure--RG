# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
 stages:
  - stage: one
  - task: CmdLine@2
    inputs:
      script: |
        echo Write your commands here
        
        echo Hello world

  - stage: two
     pool: selfhosted
     - task: CmdLine@2
     inputs:
       script: |
         echo Write your commands here
         

  - stage: three
    dependsOn:
    - two
    condition: failed('two')